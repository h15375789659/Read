# TTS语音朗读功能问题清单

## 问题列表

### 问题1：正在朗读的段落缺少视觉提示 🔄 修复中
- **描述**：当前朗读时，用户无法在主阅读界面直观看到正在朗读哪个段落
- **期望**：在小说阅读界面显示正在朗读的段落高亮，整段高亮且实时更新
- **问题细分**：
  - **滚动模式高亮闪烁后消失** ✅ 已修复：添加内容变化检测，避免覆盖高亮
  - **翻页模式高亮不显示整段** 🔄 调试中：已添加日志函数
- **调试日志**：
  - 日志标签：`TTS_HIGHLIGHT`
  - 查看方式：`adb logcat -s TTS_HIGHLIGHT:D`
  - 日志内容：
    - TTS原始位置
    - 页面原始范围
    - 转换后的页面内位置
    - 找到的段落范围
    - 高亮文本预览
- **修改文件**：
  - `ReaderActivity.java` - 添加内容变化检测，避免覆盖高亮
  - `PageContentView.java` - 添加调试日志，简化段落查找逻辑

### 问题2：音色选项不符合中国用户习惯 ✅ 已修复
- **描述**：音色选项太多且显示名称与实际语音不匹配
- **期望**：精简音色列表，只保留几个常用的有特色的声音
- **修复方案**：
  - 移除性别检测逻辑
  - 收集所有中文语音，按地区分组（普通话/台湾/粤语）
  - 每个地区最多显示2-3个语音
- **修改文件**：
  - `TTSServiceImpl.java` - 重写`loadAvailableVoices()`方法

### 问题3：段落选择起点不正确 ✅ 已修复
- **描述**：段落选择列表显示的是整章内容，而不是当前页面可见的内容
- **修复方案**：已在ReaderActivity.showTTSControlDialog()中实现

### 问题4：恢复播放不从暂停位置继续 ✅ 已修复
- **描述**：暂停后重新播放，不会从暂停的位置继续
- **修复方案**：已在TTSServiceImpl中添加startOffset变量

### 问题5：章节结尾不自动播放下一章 ✅ 已修复
- **描述**：播放到章节结尾时，不会自动切换到下一章
- **修复方案**：已在ReaderViewModel.initializeTTSObservers()中实现

### 问题6：快速翻页导致章节跳转错误 ✅ 已修复
- **描述**：在章节最后一页快速来回翻页，会导致跳转到错误的章节
- **修复方案**：
  - 添加防抖机制（800ms最小间隔）
  - 在用户开始拖动时取消待执行的章节切换
- **修改文件**：
  - `ReaderActivity.java` - 重写ViewPager2.OnPageChangeCallback

---

## 新功能

### 跳转到当前朗读位置按钮 ✅ 已修复
- **描述**：在TTS悬浮控制条添加定位按钮，点击后跳转到当前朗读位置
- **修复方案**：
  - 添加`pendingTTSJumpPosition`和`pendingTTSJumpChapterId`变量
  - 章节切换时保存待跳转位置
  - 在`updateUI`中检测章节加载完成后执行跳转
  - 添加`performTTSJump()`方法统一处理跳转逻辑
- **修改文件**：
  - `ReaderActivity.java` - 重写跳转逻辑，添加异步处理

---

## UI修复

### TTS控制对话框重复暂停按钮 ✅ 已修复
- **描述**：TTS控制对话框中有两个暂停按钮
- **修复方案**：将停止按钮的图标从`ic_media_pause`改为`ic_delete`
- **修改文件**：
  - `dialog_tts_control.xml` - 修改停止按钮图标

---

## 修复状态总结

| 问题 | 状态 | 优先级 |
|------|------|--------|
| 问题1（高亮-滚动模式） | ✅ 已修复 | 高 |
| 问题1（高亮-翻页模式） | 🔄 调试中 | 高 |
| 问题2（音色） | ✅ 已修复 | 中 |
| 问题3（段落选择） | ✅ 已修复 | 中 |
| 问题4（恢复播放） | ✅ 已修复 | 高 |
| 问题5（自动下一章） | ✅ 已修复 | 高 |
| 问题6（翻页防抖） | ✅ 已修复 | 高 |
| 跳转按钮 | ✅ 已修复 | 中 |
| 重复暂停按钮 | ✅ 已修复 | 低 |

---

**滚动模式高亮和跳转按钮已修复，翻页模式高亮正在调试中**

**调试翻页模式高亮问题：**
请在测试时使用以下命令查看日志：
```bash
adb logcat -s TTS_HIGHLIGHT:D
```

日志将显示：
- TTS原始位置
- 页面原始范围
- 转换后的页面内位置
- 找到的段落范围
- 高亮文本预览

请将日志内容反馈给我，以便进一步分析问题。
